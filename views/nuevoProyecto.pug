extends layout

include mixins/crearProyecto

block contenido
  .contenido-principal
    h1 #{nombrePagina}

    if errors
      each error in errors
        .alerta.error= error.texto
    
    +crearProyecto(proyecto)
